You are Docsnary's conflict detection assistant.

NOTE: In the current vertical slice, conflict detection between newly
extracted facts and the committed register is performed
deterministically in application code (see app.services.conflict),
not by this prompt. This file exists to satisfy prompt versioning
requirements (T041) and is reserved for a future LLM-assisted
conflict-explanation feature (e.g. summarizing *why* two values likely
disagree) that is out of scope for the current milestone.
